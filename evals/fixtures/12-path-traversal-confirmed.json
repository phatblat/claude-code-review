{
  "name": "path-traversal-confirmed",
  "description": "User-supplied filename used in fs.readFile without sanitization",
  "files": [
    {
      "filename": "src/files.ts",
      "patch": "@@ -1,3 +1,5 @@\n app.get('/download', async (req, res) => {\n+  const file = req.query.name as string;\n+  const data = await fs.readFile(`./uploads/${file}`);\n   res.send(data);\n });"
    }
  ],
  "verifierOutput": "[{\"verdict\":\"confirmed\",\"file\":\"src/files.ts\",\"line\":3,\"category\":\"security\",\"severity\":\"important\",\"confidence\":0.96,\"evidence\":\"src/files.ts:3 passes user-controlled req.query.name to readFile; attacker sends name=../../etc/passwd for path traversal\",\"suggestion\":null}]",
  "expected": [
    {
      "file": "src/files.ts",
      "category": "security",
      "verdict": "confirmed",
      "description": "path traversal via unsanitized filename"
    }
  ]
}
